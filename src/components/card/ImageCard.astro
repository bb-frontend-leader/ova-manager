---
import { Image } from "astro:assets";
const { src, alt, width, height } = Astro.props;
---


<Image  src={src} alt={alt} width={width} height={height} class="w-full h-auto border-2 borer-border"/>
